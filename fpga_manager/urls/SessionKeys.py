# This file contains all custom keys used by the app during a session

# Keys used for the reservation procedure
RESERVATION_START_DATE = "reservation_start_date"
RESERVATION_END_DATE = "reservation_end_date"
RESERVATION_REGION_TYPE = "reservation_region_type"
